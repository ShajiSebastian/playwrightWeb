import { test, expect } from '@playwright/test';

// By default, test files are run in parallel. Tests in a single file are run in order,
// There is no guarantee about the order of test execution across the files
// However, if you disable parallelism, you can control test order by either naming your files in alphabetical order or using a "test list" file.
// By naming file eg: 001-user-signin-flow.spec.ts, 002-create-new-document.spec.ts and so on.
// By putting file names in another file and calling this file
// https://playwright.dev/docs/test-parallel#control-test-order



